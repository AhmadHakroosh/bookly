export const metadata = { title: "Conferencing" };

export default function ConferencingPage() {
  return (
    <div className="space-y-2">
      <h1 className="text-2xl font-semibold tracking-tight">Conferencing</h1>
      <p className="text-sm text-muted-foreground">
        Connect Google Meet, Zoom or Microsoft Teams so meeting links are added to bookings
        automatically. Coming soon.
      </p>
    </div>
  );
}
